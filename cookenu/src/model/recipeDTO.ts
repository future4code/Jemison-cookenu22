export interface RecipeInputDTO{
    id: string
    title: string
    description: string
    createdAt: string
}

export interface InsertRecipeInputDTO{
    id: string
    title: string
    description: string
    createdAt: string
}
